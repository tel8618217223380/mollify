These scripts are intended to be run in the Mollify root (see below), copy these there first.

Correct directory structure is:
- ROOT
  - build
  - svn
    - backend
    - script (this directory)
    - src

